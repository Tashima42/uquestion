class CardEntitie {
  // maybe change to Field class in the future to support more formats
  final String front;
  final String back;
  final int id;

  CardEntitie({required this.front, required this.back, required this.id});
}
